from rsyncr import message


def test_message_send():
    # can't actually test anything, because I don't want to mock up reading the message, but...
    ## message.send("from rsyncr.tests.test_message_send: Boognish")
    pass
